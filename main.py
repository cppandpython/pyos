import sys;sys.dont_write_bytecode=True;from pprint import pprint;from time import time;time_start=time();import pyos
# В класах использовать __slots__ = () для атрибутов класса и все классы должны подержывать with
# !!!Писать без повторени и эффективный код!!!
# invalid_type('', 1.5, {'int'})  типы только передаются set
# по пробывать сделать запуск от прав системы








# with open('../pyos.zip', 'rb') as f:
#     pyos.tool.Clipboard.copy(str(f.read()))
print('\n\ntime:', str(round(time() - time_start, 2)) + 's', flush=True)
